# mystery-db

国内外の推理小説・ミステリ作品を著者・名探偵・翻訳者・出版社・受賞歴・テーマから検索できるファンデータベース。姉妹サイト[らのべDB](https://izenmi.github.io/ranobe-db/)(`izenmi/ranobe-db`)のミステリ版として作成した。アーキテクチャ・デザインシステム・運用ノウハウの多くをranobe-dbから移植している。**シリーズ探偵を独立したエンティティとして持ち、探偵ごとに登場作品を発表年順で辿れる**のが差別化点。

- 公開URL: https://izenmi.github.io/mystery-db/
- リポジトリ: `izenmi/mystery-db`(public。GitHub Pagesは無料枠だとpublicでないと使えない)
- スタック: React 18 + TypeScript + Vite 5 + `react-router-dom`(`BrowserRouter`)。ranobe-dbと異なり最初からBrowserRouterで作っているため、旧HashRouter互換のリダイレクト処理は存在しない(manga-db/game-dbと同じ)

## ネタバレ方針(このサイト固有・最重要)

推理小説を扱う以上、これが他の姉妹サイトにない最優先ルールになる。

- **あらすじ・探偵の`description`・`sourceNote`に、犯人・真相・トリックの核心を書かない**。あらすじは「読者が本を手に取るかどうか判断できる情報」までにとどめる
- **`ThemeSource.spoiler: true` を付ける基準**: 「そのタグが作品に付いていることを知ること自体が、真相の推理に直結するもの」だけ。帯・あらすじ・レーベルの惹句で公表されている構造タグは対象外
  - spoiler扱いにする: 叙述トリック、信頼できない語り手、多重解決、意外な犯人
  - spoiler扱いにしない: 密室、館もの、クローズドサークル、読者への挑戦状、倒叙、見立て殺人、孤島(いずれも売り文句として表に出ている)
- **UI側の扱い**(実装済み):
  - `WorkCard`(作品一覧・各詳細ページの関連作品)は spoiler タグを**描画しない**。一覧をスクロールしただけで漏れるのが最悪のため、`generate-manifest.mjs` が各Workに `spoilerThemeIds` を付けて出力し、カード側でそれを除外している
  - `WorkDetailPage` は「ネタバレを含むタグを表示(N件)」ボタンを押して初めて表示する。展開状態は`useState`のみで保存せず、ページを開き直すと必ず閉じた状態に戻る
  - `WorkDetailPage` のJSON-LD `genre` からも spoiler タグを除外している(検索結果のスニペットは、誰も見ることに同意していない場所のため)
  - `ThemeListPage` は spoiler タグを別セクション「ネタバレを含むタグ」にまとめる。タグ名自体は隠さない(タグ名だけでは特定作品と結びつかないため)
  - `ThemeDetailPage` は spoiler タグのページに警告バナーを出し、meta descriptionを作品数に触れない汎用文にする
  - `HomePage` の「人気テーマ」からは spoiler タグを完全に除外する

## データフロー(source → generated)

- `public/data/source/*.json` … 手作業で作成・**コミットする**一次データ(works/authors/detectives/translators/publishers/themes/awards)
- `public/data/generated/*.json` … `scripts/generate-manifest.mjs` がビルド時に生成する非正規化データ。**`.gitignore`対象**、`predev`/`prebuild`npmスクリプトで毎回再生成するので手で編集しない
- 生成スクリプトの検証(いずれも失敗するとビルドが落ちる):
  - 全Workの`authorIds`(空配列不可)/`detectiveIds`/`translatorIds`/`publisherId`/`themeIds`/`awardResults[].awardId` の参照整合性
  - `DetectiveSource.creatorAuthorId` が `authors.json` に存在すること、`firstAppearanceWorkId` が指定されていれば `works.json` に存在すること
  - **`origin` の整合性**: `"overseas"` なら `translatorIds` が1件以上あり `originalTitle` があること / `"jp"` なら `translatorIds`・`originalTitle`・`jpPublishedYear` がいずれも空であること
- 著者・探偵・翻訳者・出版社・テーマの詳細ページは、それぞれの作品一覧を`WorkGenerated`型でフル展開して埋め込む(`WorkCard`をそのまま再利用できるようにするため)

### 転送量の設計(2026-08-12。**作品をフル展開して埋め込まない**)

著者・探偵・翻訳者・出版社・テーマ・シリーズの各生成ファイルは作品を **`workIds`(id配列、
発表年の古い順)** で持ち、表示側は `getWorksByIds()`(works.json の取得済みキャッシュ)から引き直す。
あらすじと出典メモは作品詳細でしか使わないので **`work-texts.json`** に分けてある。

以前は作品をフル展開して埋め込んでいたため1作品が平均8つのリストに重複して入り、
`themes.json` だけで gzip 1.48MB あった。現在は gzip で works 334KB / work-texts 222KB /
themes 36KB / authors 52KB / detectives 21KB / publishers 21KB / translators 16KB / series 9KB。

- **新しい生成ファイルに作品を埋め込みたくなったら、まずidで足りないかを疑う**
- **作品詳細ページはあらすじが揃うまで「読み込み中」を出し続ける**こと。`prerender.mjs` は本文から
  「読み込み中」が消えるのを待って静的HTMLを書き出すので、先に描くとあらすじ抜きのHTMLと
  meta description が焼き付く
- **`useMemo` の依存配列に注意**。エンティティのstateだけを見ていると、後から解決する作品配列で
  再計算されず一覧が空になる(sf-dbのテーマ詳細で実際に踏んだ)

## データモデル上の判断(ranobe-dbとの違い)

- **1作品(1タイトル)単位**で登録する。ranobe-dbはシリーズ単位だが、ミステリはシリーズものでも各巻が独立作品として認知されており、探偵軸で発表順に並べる設計とも噛み合う。副次的に、表紙取得がタイトル完全一致で効くため精度が上がる
- **`firstPublishedYear` は原著の発表年**。海外作品もこの欄に原書刊行年を入れることで、ホームズ(1887年)から現代作までを推理小説史の時系列で並べられる。邦訳初刊年は `jpPublishedYear` に分離する
- **`seriesName` はエンティティ化しない**。ソースは works.json の自由文のままで、
  `generate-manifest.mjs` が `generated/series.json`(`SeriesGenerated`)を組み立て、
  `/series`(一覧)と `/series/<シリーズ名>`(詳細)のページと絞り込みを賄う(2026-08-12に追加。
  sf-dbで作ったものの移植)。作品側で名前を直せばそのままページになるので、シリーズ用のソースファイルは持たない
  - 探偵軸のブラウズ(`/detectives`)とは別物。**探偵が登場しないシリーズ**(「新宿鮫シリーズ」のような
    警察小説、ノンシリーズ作家の三部作)や、**同じ探偵が複数シリーズにまたがる場合**を拾えるのがシリーズ軸
  - **URLのパスにシリーズ名をそのまま載せる**(リンク側で `encodeURIComponent`、
    ルート側で `decodeURIComponent`)。`prerender.mjs` は出力先ディレクトリ名だけデコードして
    `dist/series/<日本語>/index.html` を書く。**`/` を含む名前は付けられない**
  - **表記ゆれは統合してから登録する**。中黒あり・なし(「ハリー・ボッシュ・シリーズ」と
    「ハリー・ボッシュシリーズ」)が並ぶと別シリーズになる。**多数派は中黒なしの「◯◯シリーズ」**
    (155件 対 32件)なのでそちらに寄せる。2026-08-12に16作品ぶん7シリーズを統合した。
    ただし「ドルリー・レーン四部作」「館四重奏」のような四部作の固有名にはシリーズを付けない
  - シリーズ詳細の既定の並び順だけ**刊行年の古い順**にしてある(第1作から読むものなので)。
    1作しかないシリーズもページは作るが、一覧の既定表示と絞り込みの選択肢からは外す
  - 既知の不整合: 『悪果』(黒川博行)の `seriesName` が「疫病神シリーズ関連」になっている。
    シリーズ名ではなく注記だが、正しいシリーズ名を裏取りできていない(Wikipediaに記事なし)ため
    そのままにしてある。疫病神シリーズの作品を登録するときに合わせて直すこと
- **`mediaMix` は `{ movie, drama, anime, comic }` の4種**。このジャンルは実写映画化・テレビドラマ化が主役なので、ranobe-dbのアニメ/コミカライズ2種から拡張した
- `DetectiveGenerated.works` は**`firstPublishedYear` 昇順固定**(他のエンティティ詳細ページと違いソート切替を用意していない)。「シリーズをどの順で読めばいいか」が探偵ページの存在意義のため
- `PersonKind` は `"author" | "translator" | "publisher"`。探偵は固有フィールド(生みの親・職業・初登場作)を持つため generic な `PersonListPage`/`PersonDetailPage` を使わず、`src/ui/detectives/` に専用ページを置いている(game-dbの`CompanyListPage`/`CompanyDetailPage`と同じ方針)

## データ入力ルール(ranobe-dbから踏襲)

- **出典は日本語版Wikipediaを基本とするが必須ではない**。Wikipediaに記事がない作品も登録してよく、その場合は出版社公式サイト・電子書店等の書誌情報・信頼できる他の情報源を使ってよい。ユーザーが口頭で伝えるタイトル・著者名・刊行年等はしばしば誤っているので、書き込む前に必ず何らかの情報源で裏取りする。矛盾があれば訂正し、`sourceNote`に何を確認したか・どの情報源を使ったか・何が未確認かを明記する
- **あらすじはコピペ禁止**。Wikipediaの文章表現をそのまま転記せず、150〜250字程度で必ず自分の言葉で要約する(事実自体は著作権保護対象外だが、文章表現はCC BY-SA 4.0の対象になりうるため)。加えて上記のネタバレ方針を守る
- **実在確認できない候補は無理に埋めない**。目標作品数に届かなくても、確認できたタイトルのみ収録する
- **表紙画像は`covers-cache.json`にあれば実画像、なければプレースホルダー**。直リンクの画像URLを推測・ハードコードすることはしない
- **購入リンクは検索URL形式のみ**。個別商品ページへの直リンクは使わない。理由: 1作品が単行本・文庫で別々に版を重ねるため版ごとのISBN/ASINを持てない。`amazonSearchUrl(title, extra?)`(`src/ui/common/WorkCover.tsx`)がアフィリエイトタグ`izenmi-22`(姉妹サイト共通)付きの検索URLを生成する。作品詳細ページでは第2引数に著者名を渡し、短いタイトルでも目的の本に当たるようにしている。リンクの文言は「Amazonで購入」(2026-08-05に姉妹サイト3サイトで統一)
- 新規idを追加する前に既存の`authors.json`/`detectives.json`/`translators.json`/`publishers.json`を確認し、同一人物の重複登録を避ける

## データ拡充時の作業フロー

シードデータの拡充は**必ず小バッチ(10〜15作品程度)で作業し、バッチごとに即コミット・push**する。理由: セッションのトークン/時間制限で作業が中断されても、それまでの成果を失わないため。

1. 候補作品をリストアップ
2. サブエージェントにWikipedia調査を依頼(事実確認・訂正・あらすじ要約案の作成)し、コード編集はさせない。**並列実行はしない**(ranobe-dbで2026-08-03にユーザーから明確な指示があり、姉妹サイトにも同じ方針を適用する)。1つずつ順番に起動・完了を待ってから次を起動する
3. 調査結果を `scripts/apply_batch.py` で反映する。batch.jsonのキーは `newAuthors` / `newDetectives` / `newTranslators` / `newPublishers` / `newThemes` / `newAwards` / `works`。このスクリプトは `origin` ごとの必須項目や `creatorAuthorId` の参照も `generate-manifest.mjs` と同じルールで検証し、通らない要素はレポートしてスキップする
4. `npm run build`(内部で`generate-manifest`が整合性チェックを行う)が通ることを確認
5. `git add public/data/source && git commit && git push`

## 受賞歴(awards)の方針

文学賞・新人賞に加え、ranobe-dbと同様に年間ランキング系も`awardResults`に含める。scaffold時点で登録済み: 日本推理作家協会賞、本格ミステリ大賞、直木三十五賞、山本周五郎賞、メフィスト賞、鮎川哲也賞、角川学園小説大賞、『このミステリーがすごい!』年間ランキング、週刊文春ミステリーベスト10、本格ミステリ・ベスト10、ミステリが読みたい!。

**カウントする/しないの基準:**
- 作品自体の受賞・順位が明記されているものだけを採用する。**候補・ノミネートは登録しない**(『獄門島』の探偵作家クラブ賞候補、『占星術殺人事件』の江戸川乱歩賞最終候補、『火車』の直木賞候補、『容疑者Xの献身』のエドガー賞候補などはいずれも`sourceNote`に記載するのみ)
- **映画版・ドラマ版のみが対象の賞**は小説自体の受賞ではないため対象外(『砂の器』の毎日映画コンクール大賞、『白夜行』のテレビドラマアカデミー賞など)
- **著者の別作品での受賞**と**本作自体の受賞**を明確に区別し、`sourceNote`に明記する
- 賞の名称が時代とともに変わっている場合は既存idを再利用し、`sourceNote`に当時の名称を明記する。実例: 『本陣殺人事件』の第1回受賞は当時「探偵作家クラブ賞」の名称であり、`mystery-writers-japan`(日本推理作家協会賞)のidで登録している
- ランキング系の`year`は「◯年版」の表記に合わせる(『このミステリーがすごい!2013年版』は`year: 2013`)。『週刊文春ミステリーベスト10』は発表年をそのまま使う

## 『このミステリーがすごい!』ランキングの取り込み(2026-08-07)

Wikipedia日本語版「[このミステリーがすごい!](https://ja.wikipedia.org/wiki/このミステリーがすごい!)」の
「各年のランキング」節から、**1988年版〜2026年版の国内編・海外編トップ10を機械抽出**して
`awardResults`(`kono-mystery-ga-sugoi`)に取り込む。772件が取れる。

- 節構造は `=== YYYY年 ===` → `; 国内編` / `'''国内編'''`(年によって書式が違う) → `# タイトル（著者）`。
  **順位は箇条書きの位置**で、同順位は `<br />` で1行に複数作品、欠番は `-` で埋められている
- **`==`(レベル2)の見出しで年をリセットすること**。最新年の節の後ろに「10周年ベスト・オブ・ベスト」
  「20周年…」が続き、そこでも `; 国内編` が現れるため、リセットしないと**過去の名作が最新年版の
  ランキングとして登録されてしまう**(実際に踏んで修正した)。これらの周年ベスト投票は年間ランキングとは
  別物なので取り込んでいない
- **1990年版は同ページに存在しない**(節が飛んでいる)。既存データに1990年版の順位があってもこの抽出では
  再現できないので消さないこと
- **突合は括弧併記に注意**。『64（ロクヨン）』のように読みが括弧で併記されるので、
  「括弧を外した形」「括弧の中身だけ」でも works.json と突き合わせる
- `result`の表記は **`YYYY年版 国内編 第N位`** に統一した(過去は「第1位」「国内編第1位」等が混在していた)。
  ただし11位以下の順位は上記ページに載っていないため、既存の「第16位」のような記述はそのまま残している

## テーマタグの方針

再利用可能な少数タグに絞る(1作品あたり4〜5個が目安)。scaffold時点で通常タグ30件+spoilerタグ4件。作品数0のタグがいくつか残っているのは、今後の追加で使う語彙をあらかじめ用意しているため(時代ミステリ・倒叙・読者への挑戦状・法廷ミステリ・多重解決)。新規作品を追加する際、既存タグで表現しきれない要素があれば追加してよいが、**新規タグにspoilerフラグを付けるかは上記の基準で必ず判断する**。

## デザイン方針

- パステルカラー基調、グラデーションはなるべく使わない。**メインアクセントは藤色(`--color-iris` / `--color-iris-strong` / `--color-iris-deep`)**。ranobe-dbの水色・manga-dbのオレンジ・game-dbのグリーンと区別するための独自トリオで、装飾用パステル(pink/mint/yellow/peach/blue)のローテーションとは分けている
- `--color-primary`(リンク・ブランド用の紫)とアクセントの藤色は別変数。隣り合ったときに溶けないよう色を離してある
- ページ背景は黒一色固定(`:root`で不変。`[data-theme="light"]`のパステル明テーマ定義は残しているが現状UIから到達不能=未使用のドーマント状態)
- 装飾(影・グラデーション・点線ボーダー等)は基本つけない。「真っ黒でよい」「シャドウも微妙」など過剰な装飾は嫌われる傾向
- 見出しは`M PLUS Rounded 1c`、本文は`Noto Sans JP`
- PC画面の余白を無駄にしない(`.work-grid`は`repeat(auto-fill, minmax(min(450px, 100%), 1fr))`、`.page`は`max-width: 1200px`)

## コマンド

```sh
npm install
npm run dev       # http://localhost:5173/mystery-db/
npm run build      # 型チェック + データ整合性チェック + ビルド + プリレンダー
npm run preview
npm run fetch-covers
```

`main`へのpushで`.github/workflows/deploy.yml`が自動ビルド・GitHub Pagesデプロイを行う。

## 表紙画像

`scripts/fetch-covers.mjs` は ranobe-db 版をベースに、3段フォールバック(楽天ブックス → 楽天Kobo → BOOK☆WALKER)をそのまま引き継いでいる。ミステリ向けの変更点:

- **全経路で著者名一致を必須にしている**(ranobe-dbは緩いキーワードで当たった候補にのみ課していた)。『点と線』『火車』のようにタイトルが短く一般的な作品が多く、前方一致だけでは無関係な本・同名映画のノベライズ・コミカライズが混ざるため。manga-dbが同じ理由で導入したロジックの移植
- 楽天ブックスのジャンル除外に **`001017`(ライトノベル)** を追加(`001001` コミック除外はranobe-dbから継承)。BOOK☆WALKERの `BW_REJECTED_GENRES` にも「ライトノベル」を追加
- 検索キーワードは**邦題**を使う(原題では日本の書誌に当たらない)
- **楽天の認証情報は姉妹サイトと共用できる**(現行の新gateway形式は各サイトのRefererから通ることを2026-08-04にmystery-dbでも実証済み)ので新規アプリ登録は不要。`RAKUTEN_APP_ID`/`RAKUTEN_ACCESS_KEY` を渡さずに実行すると BOOK☆WALKER のみで解決する
- **邦訳版の書影は、works.jsonに登録した版と別レーベルになることがある**。1作品1エントリの設計上、表紙はタイトルで解決するため、複数の訳者・レーベルから出ている海外古典では楽天が別の版を拾う。作品としては同一なので採用してよいが、`covers-cache.json`の`note`に食い違いを書き残すこと。実例: 『緋色の研究』(登録は新潮文庫・延原謙訳 → 書影は創元推理文庫の新訳版)、『Yの悲劇』(登録はハヤカワ・ミステリ文庫・宇野利泰訳 → 書影は角川文庫版)、『D坂の殺人事件』(登録は光文社 → 書影は創元推理文庫の江戸川乱歩全集2)
- **江戸川乱歩・横溝正史のように漫画版が多数ある作家は、書影が漫画版でないか必ず目視すること**。『D坂の殺人事件』はBOOK☆WALKERの検索上位が『漫画 D坂の殺人事件』『漫画 心理試験』で埋まるが、ジャンル判定(「マンガ（漫画）」を除外)が効いて小説版が採用された。画像を実際に開いて確認済み
- **フラグ**: `--only=id1,id2` / `--force` / `--retry-misses`。**未解決分の再挑戦には必ず`--retry-misses`を使う**こと(`--force`は手動修正済みのエントリも上書きするため)
- 古い海外古典や絶版作品は3ストアのいずれにもないことがある。その場合はプレースホルダーのままにし、無理に埋めない

## SEO / SSG

ranobe-dbの構成をそのまま移植している。

- `src/ui/common/useSeo.ts`: `document.title`・meta description・canonical・OGP/Twitterカード・JSON-LD構造化データを`useEffect`内のDOM操作で設定する。**canonical/og:urlは`window.location.origin`ではなく固定の`SITE_ORIGIN`定数から組み立てる**(`scripts/prerender.mjs`はローカルの`vite preview`からページを取得するため、`window.location.origin`を使うとプリレンダー結果のcanonicalが`localhost`になる。ranobe-dbで実際に踏んだバグ)
- JSON-LD: 作品詳細=`Book`、著者・翻訳者・探偵=`Person`、出版社=`Organization`、加えて`BreadcrumbList`。トップは`WebSite`+`SearchAction`
- `scripts/prerender.mjs`(npm `postbuild`フック): `vite build`後に`vite preview`を起動してPlaywright(Chromium)で全ルートをクロールし、`dist/<route>/index.html`を書き出す。最後に`dist/index.html`を`dist/404.html`にコピーしてGitHub Pagesのフォールバックにする。CI(`.github/workflows/deploy.yml`)では`npx playwright install --with-deps chromium`を`npm run build`の前に実行している
- `public/sitemap.xml`: `scripts/generate-manifest.mjs`の末尾で生成(`.gitignore`対象)
- `public/robots.txt`: GitHub Pagesのプロジェクトページではオリジンルートのrobots.txtが優先されうるため、確実に索引させたい場合は Google Search Console にsitemapを手動登録するのが確実(ユーザー自身のGoogleアカウント操作が必要)
- `scripts/generate-ogp.mjs`(手動実行): Playwrightで1200×630のブランドバナーを生成し`public/og-image.png`に置く
- `scripts/generate-icons.mjs`(手動実行): `public/favicon.svg`と同じ意匠で`public/favicon.ico`(16/32/48/64px)と`public/apple-touch-icon.png`(180px)を生成する。Playwrightのページ内でGoogle Fontsから`M PLUS Rounded 1c`を読み込んでから描画するのは、このコンテナに同フォントが入っていないため(ImageMagickで直接SVGを変換すると別のsans-serifにフォールバックする)。**四隅は不透明な黒で塗り、アルファを残さない**: 姉妹サイトのアイコンは角丸(`rx=16`)で四隅が透明になっており、タブストリップやICO consumer によっては白く合成される。そのためmystery-dbのアイコンだけは角丸をやめて全面塗りにし、`convert`にも`-alpha remove -alpha off`を渡している。意匠を変えるときは`favicon.svg`とこのスクリプトの両方を直すこと
- Google Analytics: `index.html`にGA4のgtagスニペットを直書きしている(測定ID `G-JM8SW0R904`)。**姉妹サイトはそれぞれ固有のプロパティを持つ**(ranobe-db `G-2NR0M8VN1N` / manga-db `G-01FCSJVHQX` / game-db `G-V6407CNZ8Y`)ので、サイト間でIDを流用しないこと

## データ規模の推移

24作品(初回、2026-08-04)。著者17・探偵14・翻訳者6・出版社9・テーマ34(うちspoiler 4)・アワード11。国内18作品・海外6作品。目標は30作品前後だったが、Wikipedia日本語版で刊行年・受賞歴・訳者を裏取りできたものだけを採用した結果24作品で区切った。

124作品(2026-08-04に100作品追加)。著者62・探偵40・翻訳者25・出版社13・テーマ34(変更なし)・アワード13(江戸川乱歩賞・松本清張賞を追加)。国内90作品・海外34作品。12件ずつ9バッチ(最後のみ4件)で、バッチごとに`apply_batch.py`→`generate-manifest`→表紙取得→commit/pushを回した。

**このときに固めた裏取りの型**(次回も踏襲するとよい):

- **Wikipedia APIを直接叩くのが最も安く済む**。`prop=extracts&exintro&exlimit=20`で20作品分の導入部を1リクエストにまとめられる(ただし`exintro`なしの全文取得は1リクエスト1ページに制限される)。導入部で足りないときは`prop=revisions&rvsection=0`でinfoboxのwikitextを取り、`published`/`publisher`/`発売日`/`出版社`の行だけを抽出する
- **作品単独の記事がない作品は著者記事で裏が取れる**(『殺戮にいたる病』『黒いトランク』『りら荘事件』『体育館の殺人』『密室蒐集家』『慟哭』など)。著者記事の「著作一覧」には初刊の年月・出版社・レーベルが載っており、受賞歴の節と合わせれば必要な項目がそろう
- **海外作品の訳者・訳書刊行年は楽天ブックスAPIの書誌が最も確実**。`BooksBook/Search`に`title=`を渡すと`title | author(訳者含む) | publisherName | salesDate | isbn`が取れる。**キーワードにスペースを含めると精度が落ちるので単一語のtitleで引くこと**。原著の発表年はja Wikipediaになければ英語版Wikipediaの導入部で確認した(『ドラゴン・タトゥーの女』『ボーン・コレクター』『その女アレックス』『メインテーマは殺人』)
- **`jpPublishedYear`は「本DBが採用した版の刊行年」**という既存の運用をそのまま踏襲した(『アクロイド殺し』が2003年になっているのと同じ考え方)。初訳年が別にある場合は`sourceNote`に併記する
- **ランキングの年次が資料間で食い違うときは登録しない**。実例: 『半落ち』(週刊文春ミステリーベスト10 1位。作品記事は2003年だが同年1位は『クライマーズ・ハイ』)、『シャドウ』(作品記事は2007年・著者記事は2006年)。いずれも`sourceNote`に食い違いの内容を書いて未登録にした

324作品(2026-08-04に続けて200作品追加)。著者97・探偵57・翻訳者43・出版社19・テーマ34(変更なし)・アワード14(『このミステリーがすごい!』大賞を追加)。国内250作品・海外74作品。10〜16件ずつ14バッチで、前回と同じ手順を回した。

**200作品追加で分かった追加の勘所**:

- **既存著者の未登録作から埋めるのが最も安い**。著者エンティティ・探偵エンティティを新規に作らずに済み、著者記事の著作一覧1回のgrepで10件以上の年・出版社が同時に取れる(森博嗣S&Mシリーズ9作、東野圭吾16作、江戸川乱歩5作などはこの方法で一括処理した)
- **シリーズ記事に書誌一覧があることが多い**。作品単独記事も著者記事の一覧もない場合でも、『S&Mシリーズ』『Vシリーズ』『新宿鮫シリーズ』『学生アリスシリーズ』のようなシリーズ記事に刊行年月とレーベルが載っている
- **ja Wikipediaに記事がなく楽天にも書誌がない作品は諦める**。2026-08-04の200件追加では『樽』(クロフツ)、『スペイン岬の謎』、『13・67』、湊かなえ『少女』などをこの理由で見送った。刊行年を推測で埋めるくらいなら落とす
- **探偵の`firstAppearanceWorkId`は後追いで埋める**。初登場作が未登録のうちは設定せず`sourceNote`に「初登場は◯◯だが未登録」と書いておき、のちにその作品を追加したタイミングで`detectives.json`を更新する。実際に加賀恭一郎(→『卒業』)、太刀洗万智(→『さよなら妖精』)、ミス・マープル(→『牧師館の殺人』)、エラリー・クイーン(→『ローマ帽子の謎』)をこの手順で更新した
- **上下巻ものは自動取得が下巻を拾いやすい**。`sort=+releaseDate`で同日刊行の上下巻が並ぶと下巻が先に一致することがあるため、上下巻がある作品は`matchedTitle`を必ず確認して上巻のISBNに差し替える(今回は『マークスの山』『ヨルガオ殺人事件』『ジェノサイド』『奇面館の殺人』『Another』などで発生)

524作品(2026-08-04にさらに200作品追加)。著者104・探偵67・翻訳者58・出版社22・テーマ34(変更なし)・アワード15(吉川英治文学賞を追加)。国内399作品・海外125作品。14〜18件ずつ13バッチで回した。表紙は510/524=97.3%が解決済み。

**この回で新たに固めたやり方**(次回はここから始めるとよい):

- **サブエージェントを使わず、Wikipedia APIと楽天ブックスAPIを直接叩いて自分で裏取りするのが最も安い**。調査用に2つの使い捨てスクリプトを書いて回した。同じものを作り直せばよい
  - `wp.py`: `prop=extracts&exintro&explaintext&exlimit=20` で最大20タイトルの導入部を1リクエストで取得し、各400字に切って表示する。刊行年・原題・原著発表年・シリーズ内の位置づけはたいていここで足りる
  - `wpbox.py`: `prop=revisions&rvsection=0` でinfobox行だけ、または全文から正規表現に一致する行だけを抜き出す。**著者記事の著作一覧をgrepするのがいちばん効率がよく**、`^\* *\[*(タイトル1|タイトル2|…)` のような選択パターンで10件以上の刊行年月・レーベルが一度に取れる
  - `rfix.py`: 楽天ブックスAPI(`BooksBook/Search`)の薄いラッパ。`search`(候補一覧)、`cap`(出版社紹介文の取得)、`set <workId> <isbn> <note>`(covers-cacheの差し替え)、`drop`(プレースホルダーに戻す)の4モード。**エンドポイントは`openapi.rakuten.co.jp`、認証は`applicationId`+`accessKey`のクエリパラメータ、`Referer`/`Origin`ヘッダ必須**(`app.rakuten.co.jp`＋Bearerでは400が返る。`fetch-covers.mjs`と同じ流儀に合わせること)
- **あらすじの下敷きには楽天ブックスの`itemCaption`(出版社紹介文)が使える**。Wikipediaに記事がない作品でも書誌側に紹介文があることが多く、これを読んでから自分の言葉で要約すれば事実誤認を避けられる。**ただし転記は禁止**(コピペ禁止ルールは出版社紹介文にも及ぶ)
- **`title`パラメータ検索はスペースを含めると壊滅的に精度が落ちる**。`keyword`に「タイトル 著者名」を渡すとまったく無関係な絵本セットなどが返る。単一語の`title`で引き、結果を著者名でgrepするのが正しい
- **著者名の表記揺れで`fetch-covers.mjs`が全滅することがある**。カーは楽天側が「ジョン・ディクソン・カー」「カーター・ディクスン」で、works.jsonの「ジョン・ディクスン・カー」と一致せず7件すべてmissになった。この場合は`rfix.py search`でISBNを拾って`set`で直接入れる
- **不確かなら落とす、を徹底した**。訳者が特定できなかった『緑は危険』『トレント最後の事件』『さむけ』、単行本の刊行年が確認できなかった『ヒポクラテスの誓い』、あらすじの裏が取れなかった麻耶雄嵩『あいにくの雨で』『ふたたび赤い悪夢』などは今回見送っている
- **短編集の表題作が受賞している場合は受賞歴に登録しない**。湊かなえ『望郷』(収録作「望郷、海の星」が日本推理作家協会賞短編部門)、横山秀夫『動機』(表題作が同賞短編部門)はいずれも`sourceNote`に書くだけにした。既存の「作品自体の受賞だけを採る」方針の適用例

1033作品(2026-08-06〜07に509作品追加)。著者206・探偵91・翻訳者121・出版社27・テーマ34(変更なし)・アワード16(大藪春彦賞を追加)。国内734作品・海外299作品。

**この回で導入した4本のスクリプト**(`scripts/`にコミット済み。次回はこれを使うところから始める):

- **`probe_ndl.py`** … このサイトの**主力ツール**。国立国会図書館サーチのopensearch APIを叩き、候補タイトルごとに `title/kana/resp(責任表示)/publisher/year/first_year/isbn/originalTitle` を1行で返す。**個々のミステリ小説はWikipediaに記事がないことが多く、NDLの方が圧倒的に当たる**
  - **`works.json`との重複判定を検索の前に行い、`DUP`と出た候補はネットワークアクセスすらしない**(「詳しく調べる前に登録済みか確認する」というユーザーの指示に対応する部分)。50件の候補を投げて30件がDUPで弾かれることも珍しくない
  - 候補ファイルは **`タイトル|著者`** の形式に対応している。著者は`creator`パラメータに渡して絞り込みに使うだけで、重複判定はタイトルのみで行う。**同名異作(佐々木譲『笑う警官』とマイ・シューヴァル『笑う警官』)を著者で潰してはいけない**ため
  - `mediatype`は指定しないこと(`mediatype=1`を付けると図書が0件になる)。`--sleep 2 --workers 3`が安定
- **`probe.py`** … Wikipedia版のプローブ。映像化情報やシリーズの位置づけを取りたいときに使う。**著者記事を作品記事と誤認する事故**が多発したので、(1)候補の著者名とページ名が一致したら棄却、(2)導入部の**第一文**が「〜は小説家/推理作家/評論家」で終わったら棄却、の2段構えを入れてある。判定を導入部全体に広げると『宿命 (小説)』のような正しいページまで落ちるので、第一文に限ること
- **`merge_batch.py`** … probeの出力(NDL形式・Wikipedia形式のどちらでも可。`"page"`キーの有無で判別)と手書きのannot JSONを突き合わせて`apply_batch.py`用のbatch.jsonを作る。annotのキーは短縮形(`a`=authorIds / `tr`=translatorIds / `d`=detectiveIds / `p`=publisherId / `th`=themeIds / `o`=origin(`jp`|`ov`) / `ot`=originalTitle / `fy`=firstPublishedYear / `jy`=jpPublishedYear / `syn`=あらすじ / `series` / `awards`)。`sourceNote`は出典に応じて自動生成し、末尾に必ず「あらすじは独自要約(コピペなし、真相・トリックには触れていない)。」を付ける
  - **`newDetectives`の生みの親のキーは`creator`**(`creatorAuthorId`ではない)。間違えると`KeyError`で落ちる
- **`find_people.py`** … 人名を渡すと`authors.json`/`translators.json`の既存idを返す。206人の著者・121人の翻訳者のJSONをコンテキストに読み込まずに突合できるので、**バッチごとに必ず先に通す**こと

**この回で分かったこと**:

- **NDLの`first_year`は当てにならない**。同名異作や別シリーズの書誌を巻き込んで最古の刊行年を返すため、『親指のうずき』が1929年、『Wの悲劇』が1981年になる。**`firstPublishedYear`は自分の知識か別資料で裏を取り、NDLの値はそのまま使わない**
- **NDLは漫画版・児童書版・英語学習版を平然と返す**。責任表示に「漫画」「作画」「絵」が入っていたら別物。実例: 『天久鷹央の推理カルテ』『さよなら神様』『ヘルドッグス』『土漠の花』『警官嫌い』はいずれもコミカライズの書誌だった
- **`apply_batch.py`のリジェクト理由は3種類に集中する**。(1)`duplicate work id`(既存と衝突。**この形で重複登録を8件見つけた**)、(2)`invalid: ['publisherId:...']`(未登録の出版社。扶桑社・二見書房・ヴィレッジブックスを今回追加した)、(3)`invalid: ['themeId:...']`/`['awardId:...']`(存在しないタグ・賞。`crime-underworld`のような思いつきのタグを書いてしまう事故が起きる)。**リジェクトされた分は捨てずに、原因を直して`{"works":[...]}`だけの小さなbatch.jsonで再投入する**
- **`originalTitle`での重複検出が海外作品には効く**。邦題が『エジプト十字架の秘密』と『エジプト十字架の謎』のように訳し分けられていると`works.json`のタイトル比較をすり抜けるため、追加後に`originalTitle`の重複を集計して確認するとよい(今回これで6件の二重登録を発見した)
- **あらすじに非日本語の断片が混入する事故が繰り返し起きる**。ロシア語・韓国語・英単語が紛れ込むので、書き込み前に `set(re.findall(r"[Ѐ-ӿ가-힯]", s))` で点検し、英単語も目視すること
- **`npm run fetch-covers`は10分のBashタイムアウトを超える**。`run_in_background: true`で起動し、バッチ作業を続けながら完了通知を待つ運用にした

## 関連作品レコメンド「この作品が好きなら」(2026-08-06実装)

作品詳細ページの末尾(`source-note`の直前)に、似た作品を6件表示する。姉妹サイト4サイトすべてに
同一パターンで実装済み。

- **スコアはビルド時に`scripts/generate-manifest.mjs`で計算**し、`generated/works.json`の
  各要素に`relatedWorkIds: string[]`(スコア降順)として持たせる。実行時の計算はしない
- **テーマタグのIDF重み付きコサイン類似度**を使う。テーマタグの語彙は意図的に少なく使い回す方針
  (「テーマタグの方針」の節)なので、共通タグ数を単純に数えると「密室」のような多数の作品が持つタグに
  引っ張られて、どのページでも同じ作品が並んでしまう。IDFで希少なタグを重く見ることでこれを避ける。
  全作品が持つタグはidf=0になり自動的にスコアから消える
- **著者が共通なら+0.15、シリーズ探偵が共通なら+0.10のボーナス**。タグの重なりが薄い場合でも同じ著者の作品は有力な候補なので、
  タグ共通ゼロでも候補に入れる(ボーナスは最大0.15でコサイン類似度の最大1.0より小さいため、
  タグが強く一致する作品を押しのけることはない)
- **同点はid昇順で決める**。プリレンダー結果がビルドごとに変わらないようにするため
- **spoilerタグはスコア計算から完全に除外する**(このサイト固有)。叙述トリックや意外な犯人で
  作品をグルーピングすると、レコメンド欄自体が真相のヒントになってしまい「ネタバレ方針」に反する。
  `WorkCard`がspoilerタグを描画しないのと同じ理由
- **`relatedWorkIds`は`generated/works.json`にしか入れない**。各作品は著者・探偵・翻訳者・テーマ等の
  相互参照リストに何度も埋め込まれるため(`fullWork()`)、そこから除外しないと
  生成JSONが大きく膨らむ。詳細ページは`getWorks()`(取得済みキャッシュ)から解決するので追加の通信は発生しない
- チューニング用の定数は`generate-manifest.mjs`冒頭の`RELATED_COUNT`と各ボーナス値


## 原作↔コミカライズ相互リンク(2026-08-06実装)

姉妹サイト間で同一原作の作品ページを相互リンクする。`WorkSource.relatedComicUrl`にまんがDBの作品ページURLを持つ。

- **突合スクリプトは manga-db の `scripts/link-sister-works.mjs` に1本だけ置いてある**(manga-dbが
  2つの小説サイト両方にリンクする唯一のハブのため)。**手動実行**で、3リポジトリの
  `public/data/source/works.json` を直接書き換える。姉妹リポジトリは別のgitリポジトリなのでCIからは
  見えない。**いずれかのサイトに作品を追加したら手元で再実行してコミットすること**
  (`node scripts/link-sister-works.mjs --dry-run` で書き込まずに結果だけ確認できる)
- **突合条件は2つとも必須**: (1) 記号・副題を落として正規化したコアタイトルが一致、
  (2) コミック側の**原作者**が小説側の著者と1人以上一致。条件(2)が安全装置で、
  『殺戮の天使』は同名だが原作者が別人(小説=木爾チレン / コミック原作=真田まこと)の
  **無関係な作品**なので正しく除外される。タイトルだけで突合すると誤リンクになる
- **同じ小説が2つの姉妹サイトに登録されていることがある**。『虚構推理』(城平京・講談社タイガ)は
  ranobe-db と mystery-db の両方にあり、`relatedNovelUrl`は単数なので人間が置き場所を選ぶ必要がある。
  スクリプト内の`PREFERRED_SITE_BY_COMIC_ID`に理由つきで書き、未登録の衝突は**エラーで停止**する
  (黙って片方を選ばない)
- リンクは毎回ゼロから計算し直すので、タイトル修正などで一致しなくなった作品からは古いリンクが消える
- **姉妹サイト名はURLから導く**(`/mystery-db/`を含むか)。別フィールドに持つと二重管理になるため
- 2026-08-06時点の突合結果: **36件**(ranobe-db 31件 / mystery-db 5件)


## 既知の未着手事項

- ~~**楽天ブックス経由の表紙取得が未実行**~~ → **2026-08-04に解消。24作品中24作品(100%)が解決済み**(内訳: BOOK☆WALKER 16 / 楽天ブックス 8)。最初にBOOK☆WALKERのみで16件を解決したあと、ユーザーから楽天の認証情報を受け取り`--retry-misses`で残り8件を解決した。紙の書籍が中心の古典・文庫作品(本陣殺人事件・獄門島・火車・64・満願・氷菓・Yの悲劇)は楽天ブックスの方が明確に強い。
  - **BOOK☆WALKERの誤マッチ実例(対処済み)**: 『緋色の研究』でBOOK☆WALKERが『【大活字シリーズ】英語原文で味わうSherlock Holmes1』(英語原文の学習用版)を拾っていた。著者名一致・ジャンル判定を通り抜けるタイプの誤マッチなので、`matchedTitle`の目視確認は省略しないこと。**`--force`で回すとこの種の誤マッチが復活するので、再挑戦には必ず`--retry-misses`を使う**
- **新人賞 / ランキング系のフィルター**: 受賞歴を種別で区別してフィルターする機能は未実装(ranobe-dbと同じ課題)。`awards.json`に賞の種別フィールドの追加が必要
- **表紙が未解決の14作品**(2026-08-04時点、524作品中510作品=97.3%が解決済み): 『悪意』『眠りの森』『どちらかが彼女を殺した』『名探偵の掟』『十字屋敷のピエロ』『変身』『同級生』『天使の耳』『虚ろな十字架』(いずれも東野圭吾)、『GOTH リストカット事件』(乙一)、『連鎖』(真保裕一)、『能面殺人事件』(高木彬光)、『レッド・ドラゴン』(トマス・ハリス)、『暗い鏡の中に』(ヘレン・マクロイ)。**東野圭吾の講談社系の一部作品は楽天ブックス・Kobo・BOOK☆WALKERのいずれにも書誌がなく、WikipediaのISBNを直接指定してもヒットしない**という共通の傾向がある。海外の絶版古典(『暗い鏡の中に』は1955年のハヤカワ・ミステリ)も同様。プレースホルダーのままにしている
- **表紙が別版・別巻になっている作品はcovers-cacheの`note`に理由を書いてある**。今回追加分では『十万分の一の偶然』(単著の書誌がなく『松本清張全集 第43巻』の書影)、『プレイバック』(清水俊二訳を登録、書影は村上春樹訳版)、『エンプティー・チェア』『石の猿』(Koboに下巻しかない)が該当する
- **タイトルが短い・副題付きの作品は自動取得が外しやすい**。2026-08-04の追加では『月光ゲーム Yの悲劇\'88』(副題が書誌側にない)、『幻の女』、『マークスの山』(下巻がヒット)、『Xの悲劇』『奇岩城』(登録した訳者と別の版がヒット)などをISBN直指定で手動修正した。修正には`covers-cache.json`の該当エントリを楽天の`BooksBook/Search?isbn=`の結果で置き換え、`note`に「なぜ差し替えたか」を必ず書き残す運用にしている
- **英語学習用・児童向けの版を拾う誤マッチが海外作品で頻発する**。実例: 『ナイルに死す』でIBCパブリッシングの英語ラダー版、『シャーロック・ホームズの冒険』で講談社インターナショナルの対訳版、『ABC殺人事件』『四つの署名』で偕成社の児童向け版。CLAUDE.mdに既記の『緋色の研究』の大活字版と同じ類型なので、`matchedTitle`と出版社の目視確認は省略しないこと

## 候補の集め方: scripts/suggest_candidates.py(2026-08-08 新設)

**候補タイトルを自分で思いつくのはやめる。** 楽天ブックスのカタログに列挙させて、
works.json と突合済みの「実在していて未登録」の一覧を得る。

```sh
RAKUTEN_APP_ID=xxx RAKUTEN_ACCESS_KEY=yyy \
  python3 scripts/suggest_candidates.py out.json [--pages 5] [--sort sales] [--keyword <語>]
```

**なぜこれが要るか**: 2026-08-08にtech-dbで「ネットワーク系100冊」を思いつきの書名で
リストアップしたところ、93件中59件がNDLに存在せず、残った35件にも誤マッチが多発した
(『ルーティングの教科書』→『オウンドメディアリク**ルーティングの教科書**』、
『データセンターネットワーク』→学位論文、『分散システムの設計』→学位論文)。
カタログ側に列挙させれば、出てくるものは全て実在し、重複も事前に除ける。
game-db の `scripts/suggest-candidates.mjs`(IGDB版)と同じ発想。

**`booksGenreId` による絞り込みは当てにならない**(実測)。「パソコン・システム開発」を
指定しても『るるぶ東海オンエア』『うんこドリル』『共通テスト情報1』が混ざる。
ジャンルは足がかりにとどめ、**最終的な選別は必ずタイトルを目視して行うこと**。
`--keyword` でタイトル語を足すほうが精度が高い。

**新刊に偏る**点にも注意。`--sort sales` は売れ筋順だが結果は新しい巻に寄るので、
既刊シリーズの途中巻(『ONE PIECE 115』『キングダム 80』)が大量に出る。
このサイトはシリーズ単位で登録するため、**巻数付きタイトルはシリーズ名に丸めてから
重複判定し直す**必要がある。
